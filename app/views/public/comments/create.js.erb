$("#comments_form").html("<%= j(render "public/comments/new_comment", post: @currentpost, comment: @comment) %>");

$("#comments_index").html("<%= j(render "public/comments/index_comment", post: @currentpost) %>");