$("#comments_form").html("<%= j(render "public/comments/newcomment", post: @currentpost, comment: @comment) %>");

$("#comments_index").html("<%= j(render "public/comments/indexcomment", post: @currentpost) %>");